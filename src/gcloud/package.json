{
  "name": "@modelcontextprotocol/server-gcloud",
  "version": "0.1.0",
  "description": "MCP server for Google Cloud Platform API",
  "type": "module",
  "bin": {
    "mcp-server-gcloud": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1",
    "@types/node": "^22",
    "googleapis": "^129.0.0",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
